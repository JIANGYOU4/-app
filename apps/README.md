# Apps 项目

本目录用于存放各个 App 的源代码，每个 App 放在独立的子文件夹中。

## 建议的组织方式

```
apps/
├── app-one/    # 第一个 App 的源代码
├── app-two/    # 第二个 App 的源代码
└── ...
```

> 每个 App 子文件夹内建议包含自己的 `README.md`，说明该 App 的功能、运行方式等。
